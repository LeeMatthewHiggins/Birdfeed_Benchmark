name: benchmark
description: Rendering performance benchmark tool
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  dentity: ^1.9.0
  desktop_drop: ^0.7.0
  file_picker: ^8.1.6
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  image: ^4.0.0
  megasprite: ^0.1.0
  rive: 0.14.0-dev.13
  rive_native: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
